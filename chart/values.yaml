# Default values for OpenDSO umbrella chart
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

global:
  # Global settings shared across all subcharts
  namespace: opendso
  # Site configuration - determines which config files to use
  site: ieee13
  # Container registry settings
  imageRegistry: ""
  imagePullSecrets: []
  # Storage settings
  storageClass: standard # Use "standard" for local/minikube
  # Common labels
  commonLabels: {}
  # Common annotations
  commonAnnotations: {}
  # =============================================================================
  # Deployment Configuration
  # =============================================================================
  deployment:
    # Environment type: production, development, or staging
    environment: development
    # Cloud provider: gcp, aws, azure, or local
    cloudProvider: local
  # =============================================================================
  # Resource Profile Configuration
  # =============================================================================
  # Resource profiles provide platform-specific resource sizing presets
  # Options: minimal, default, production
  # Override individual service resources as needed
  resourceProfile: default
  resourceProfiles:
    minimal: # For local development (Minikube, Kind)
      databases:
        requests:
          cpu: 100m
          memory: 256Mi
        limits:
          cpu: 500m
          memory: 1Gi
      services:
        requests:
          cpu: 100m
          memory: 128Mi
        limits:
          cpu: 200m
          memory: 256Mi
      frontends:
        requests:
          cpu: 100m
          memory: 64Mi
        limits:
          cpu: 100m
          memory: 128Mi
    default: # For dev/staging environments
      databases:
        requests:
          cpu: 500m
          memory: 1Gi
        limits:
          cpu: 2000m
          memory: 4Gi
      services:
        requests:
          cpu: 250m
          memory: 256Mi
        limits:
          cpu: 1000m
          memory: 1Gi
      frontends:
        requests:
          cpu: 100m
          memory: 128Mi
        limits:
          cpu: 200m
          memory: 256Mi
    production: # For GKE/EKS production workloads
      databases:
        requests:
          cpu: 1000m
          memory: 2Gi
        limits:
          cpu: 4000m
          memory: 8Gi
      services:
        requests:
          cpu: 500m
          memory: 512Mi
        limits:
          cpu: 2000m
          memory: 2Gi
      frontends:
        requests:
          cpu: 100m
          memory: 128Mi
        limits:
          cpu: 500m
          memory: 512Mi
  # =============================================================================
  # Keycloak Configuration (Shared across all charts)
  # =============================================================================
  # Authentication and authorization settings
  # URLs are computed from ingress configuration at deployment time
  keycloak:
    # External URL for browser-based authentication
    # Set by deploy scripts: https://keycloak.<domain>
    url: "https://keycloak.demo-gcp.oesinc.dev" # default; overridden by --set global.keycloak.url
    # Internal URL for service-to-service communication
    # Set by deploy scripts: http://<release-name>-keycloak-svc:8080
    internalUrl: "http://opendso-keycloak-svc:8080" # default for release name "opendso"
    # Keycloak realm name
    realm: "oes"
    # Client IDs for different services
    clientIds:
      ui: "gms" # For frontend applications
      api: "gms" # For backend API (same client as UI)
  # =============================================================================
  # Frontend Application Environment Configuration
  # =============================================================================
  # URLs used by browser-based frontend applications
  # These must be externally accessible (not internal Kubernetes services)
  environment:
    # API URL for backend services (externally accessible)
    apiUrl: "https://opendso-gms-api:8000" # Override in values-dev.yaml for external access
    # NATS WebSocket URL for real-time messaging (externally accessible)
    natsUrl: "wss://opendso-nats-service-ws:9222" # Override in values-dev.yaml for external access
    # Google Analytics configuration
    googleTagId: "GTM-B7JGQS2"
    googleTagEnv: "&gtm_auth=NaiPwz5U_xGuF4hoZ7yHUw&gtm_preview=env-5&gtm_cookies_win=x"
  # =============================================================================
  # TLS Certificate Configuration
  # =============================================================================
  # OPTIONAL: Create TLS secrets via Helm templates
  # By default, secrets are created by deployment scripts (scripts/deploy-*.sh)
  # Set createSecrets: true to have Helm manage these secrets instead
  tls:
    # Enable Helm-managed TLS secrets (default: false - use script-based approach)
    createSecrets: false
    # Certificate content (base64-encoded PEM format)
    # Only used if createSecrets: true
    # These values should be populated via --set-file or CI/CD
    caCert: "" # Root CA certificate
    serverCert: "" # Server certificate
    serverKey: "" # Server private key
  # =============================================================================
  # Centralized Image Configuration
  # =============================================================================
  # All container images and tags in one place for easy version management
  images:
    # Infrastructure images
    nats:
      repository: nats
      tag: "2.12.5"
      pullPolicy: IfNotPresent
      digest: "" # sha256:xxx - when set, overrides tag
    keycloak:
      repository: quay.io/keycloak/keycloak
      tag: "24.0"
      pullPolicy: IfNotPresent
      digest: ""
    envoy:
      repository: envoyproxy/envoy
      tag: "v1.17.0"
      pullPolicy: IfNotPresent
      digest: "" # sha256:xxx - when set, overrides tag
    # Database images
    mongodb:
      repository: mongodb/mongodb-community-server
      tag: "7.0.2-ubi8"
      pullPolicy: IfNotPresent
      digest: "" # sha256:xxx - when set, overrides tag
    citusDb:
      repository: citusdata/citus
      tag: "12.1.2-alpine"
      pullPolicy: IfNotPresent
      digest: "" # sha256:xxx - when set, overrides tag
    keycloakDb:
      repository: postgres
      tag: "17.0-alpine3.20"
      pullPolicy: IfNotPresent
      digest: "" # sha256:xxx - when set, overrides tag
    appsDb:
      repository: timescale/timescaledb
      tag: "latest-pg16"
      pullPolicy: IfNotPresent
      digest: "" # sha256:xxx - when set, overrides tag
    # Core Services
    historianSvc:
      repository: historian
      tag: "latest"
      pullPolicy: IfNotPresent
      digest: "" # sha256:xxx - when set, overrides tag
    gmsApi:
      repository: gms-api
      tag: "latest"
      pullPolicy: IfNotPresent
      digest: "" # sha256:xxx - when set, overrides tag
    openfmbEventService:
      repository: openfmb-event-service
      tag: "latest"
      pullPolicy: IfNotPresent
      digest: "" # sha256:xxx - when set, overrides tag
    # Topology Services
    topologyGenesis:
      repository: topology-genesis
      tag: "latest"
      pullPolicy: IfNotPresent
      digest: "" # sha256:xxx - when set, overrides tag
    topologyNodes:
      repository: topology-nodes
      tag: "latest"
      pullPolicy: IfNotPresent
      digest: "" # sha256:xxx - when set, overrides tag
    derDispatchApp:
      repository: der-dispatch-app
      tag: "latest"
      pullPolicy: IfNotPresent
      digest: "" # sha256:xxx - when set, overrides tag
    derDispatchSvc:
      repository: der-dispatch
      tag: "latest"
      pullPolicy: IfNotPresent
      digest: "" # sha256:xxx - when set, overrides tag
    # Frontend Applications
    genesisNodeApp:
      repository: genesis-node-app
      tag: "latest"
      pullPolicy: IfNotPresent
      digest: "" # sha256:xxx - when set, overrides tag
    dataViewerApp:
      repository: data-viewer-app
      tag: "latest"
      pullPolicy: IfNotPresent
      digest: "" # sha256:xxx - when set, overrides tag
    scheduleDispatchApp:
      repository: edo-adr-app
      tag: "latest"
      pullPolicy: IfNotPresent
      digest: "" # sha256:xxx - when set, overrides tag
    eventViewerApp:
      repository: event-viewer-app
      tag: "latest"
      pullPolicy: IfNotPresent
      digest: "" # sha256:xxx - when set, overrides tag
    gisApp:
      repository: gis-app
      tag: "latest"
      pullPolicy: IfNotPresent
      digest: "" # sha256:xxx - when set, overrides tag
    historianApp:
      repository: historian-app
      tag: "latest"
      pullPolicy: IfNotPresent
      digest: "" # sha256:xxx - when set, overrides tag
    inspectorApp:
      repository: inspector-app
      tag: "latest"
      pullPolicy: IfNotPresent
      digest: "" # sha256:xxx - when set, overrides tag
    inventoryApp:
      repository: inventory-app
      tag: "latest"
      pullPolicy: IfNotPresent
      digest: "" # sha256:xxx - when set, overrides tag
    oneLineApp:
      repository: one-line-app
      tag: "latest"
      pullPolicy: IfNotPresent
      digest: "" # sha256:xxx - when set, overrides tag
    opendsoDocsApp:
      repository: opendso-docs
      tag: "latest"
      pullPolicy: IfNotPresent
      digest: "" # sha256:xxx - when set, overrides tag
    openfmbEventCreatorApp:
      repository: openfmb-event-creator-app
      tag: "latest"
      pullPolicy: IfNotPresent
      digest: "" # sha256:xxx - when set, overrides tag
    # ESS Applications
    essManagerSvc:
      repository: ess-manager
      tag: "latest"
      pullPolicy: IfNotPresent
      digest: "" # sha256:xxx - when set, overrides tag
    essManagerApp:
      repository: device-details-app
      tag: "latest"
      pullPolicy: IfNotPresent
      digest: "" # sha256:xxx - when set, overrides tag
    essTesterSvc:
      repository: ess-tester
      tag: "latest"
      pullPolicy: IfNotPresent
      digest: "" # sha256:xxx - when set, overrides tag
    essTesterApp:
      repository: batt-testing-app
      tag: "latest"
      pullPolicy: IfNotPresent
      digest: "" # sha256:xxx - when set, overrides tag
    assetHealthSvc:
      repository: ahs
      tag: "latest"
      pullPolicy: IfNotPresent
      digest: "" # sha256:xxx - when set, overrides tag
    assetHealthSimSvc:
      repository: ahs-sim
      tag: "latest"
      pullPolicy: IfNotPresent
      digest: "" # sha256:xxx - when set, overrides tag
    essManagerRedis:
      repository: redis
      tag: "7.0"
      pullPolicy: IfNotPresent
      digest: "" # sha256:xxx - when set, overrides tag
    # CVR Applications
    cvrSvc:
      repository: cvr
      tag: "latest"
      pullPolicy: IfNotPresent
      digest: "" # sha256:xxx - when set, overrides tag
    cvrGeneticAlgorithmSvc:
      repository: genetic-cvr
      tag: "latest"
      pullPolicy: IfNotPresent
      digest: "" # sha256:xxx - when set, overrides tag
    cvrOpenfmbServicesSvc:
      repository: openfmb-services
      tag: "latest"
      pullPolicy: IfNotPresent
      digest: "" # sha256:xxx - when set, overrides tag
    # OpenDSS Simulation Services
    omegadssSvc:
      repository: omegadss
      tag: "2c8e8a04"
      pullPolicy: IfNotPresent
      digest: "" # sha256:xxx - when set, overrides tag
    rpcdssSvc:
      repository: rpcdss
      tag: "3d3b0e15"
      pullPolicy: IfNotPresent
      digest: "" # sha256:xxx - when set, overrides tag
    natsAuthSvc:
      repository: nats-auth-service
      tag: "679a94b4"
      pullPolicy: IfNotPresent
      digest: "" # sha256:xxx - when set, overrides tag
  # =============================================================================
  # Component Enable/Disable
  # =============================================================================

  # Infrastructure Services
  nats:
    enabled: true
  nats-auth-svc:
    enabled: false
  keycloak:
    enabled: true
  # Monitoring & Observability
  grafana:
    enabled: true # Enable for monitoring/dashboards
  # Database Services
  mongodb:
    enabled: true
    tls:
      enabled: true
      # Auto-generate self-signed certificates (recommended for development)
      # For production, set existingSecret with proper certificates
      existingSecret: ""
      mode: preferTLS # Options: disabled, allowTLS, preferTLS, requireTLS
      allowConnectionsWithoutTLS: true
  citus-db:
    enabled: true
  keycloak-db:
    enabled: false
  # Core Services
  historian-svc:
    enabled: true
  gms-api:
    enabled: true
  openfmb-event-service:
    enabled: true
  # Topology Services
  topology-genesis:
    enabled: true
  topology-nodes:
    enabled: true
  der-dispatch-app:
    enabled: true
  der-dispatch-svc:
    enabled: true
  # Frontend Applications
  genesis-node-app:
    enabled: true
  data-viewer-app:
    enabled: true
  event-viewer-app:
    enabled: true
  gis-app:
    enabled: true
  historian-app:
    enabled: true
  inspector-app:
    enabled: true
  inventory-app:
    enabled: true
  one-line-app:
    enabled: true
  opendso-docs-app:
    enabled: true
  openfmb-event-creator-app:
    enabled: true
  schedule-dispatch-app:
    enabled: true
  ess-manager-app:
    enabled: true
  ess-tester-app:
    enabled: true
  # ESS Applications
  ess-manager-svc:
    enabled: true
  ess-tester-svc:
    enabled: true
  asset-health-svc:
    enabled: false
  asset-health-sim-svc:
    enabled: false
  opendso-apps-db:
    enabled: true
  ess-manager-redis:
    enabled: true
  # CVR
  cvr-svc:
    enabled: false
  cvr-genetic-algorithm-svc:
    enabled: false
  cvr-openfmb-services-svc:
    enabled: false
  # OpenDSS Services
  omegadss-svc:
    enabled: true
  rpcdss-svc:
    enabled: true
# =============================================================================
# OpenDSO Apps Database (Shared)
# =============================================================================
opendso-apps-db:
  auth:
    username: essuser
    password: esspassword
    database: ess_tester
    existingSecret: ""
    usernameKey: username
    passwordKey: password
    databaseKey: database
  # BYO database (marketplace-friendly)
  externalDatabase:
    enabled: false
    host: ""
    port: 5432
    database: ""
    username: ""
    password: ""
    existingSecret: ""
    usernameKey: username
    passwordKey: password
    databaseKey: database
# =============================================================================
# Historian Database (Citus)
# =============================================================================
citus-db:
  auth:
    username: citususer
    password: cituspassword
    database: ofmb_db
    existingSecret: ""
    usernameKey: username
    passwordKey: password
    databaseKey: database
  externalDatabase:
    enabled: false
    host: ""
    port: 5432
    database: ""
    username: ""
    password: ""
    existingSecret: ""
    usernameKey: username
    passwordKey: password
    databaseKey: database
# =============================================================================
# Ingress Configuration
# =============================================================================
# Unified Ingress for all UI applications and APIs
# Uses subdomain-based routing (best for SPAs and production)
#
# Prerequisites:
#   1. Install an Ingress controller (nginx, traefik, etc.)
#      For nginx: kubectl apply -f https://raw.githubusercontent.com/kubernetes/ingress-nginx/main/deploy/static/provider/cloud/deploy.yaml
#   2. Wait for controller to be ready: kubectl wait --namespace ingress-nginx --for=condition=ready pod --selector=app.kubernetes.io/component=controller --timeout=120s
#
# Access URLs after enabling (subdomain-based):
#   http://keycloak.demo-gcp.oesinc.dev     → Keycloak user management
#   http://genesis.demo-gcp.oesinc.dev      → Genesis Node App
#   http://gis.demo-gcp.oesinc.dev          → GIS App
#   http://events.demo-gcp.oesinc.dev       → Event Viewer
#   http://inventory.demo-gcp.oesinc.dev    → Inventory Management
#   http://event-creator.demo-gcp.oesinc.dev → OpenFMB Event Creator
#   http://data-viewer.demo-gcp.oesinc.dev  → Data Viewer
#   http://der-dispatch.demo-gcp.oesinc.dev → DER Dispatch
#   http://ess-manager.demo-gcp.oesinc.dev  → ESS Manager
#   http://api.demo-gcp.oesinc.dev          → GMS API
#
# Setup /etc/hosts for local development:
#   192.168.49.2 demo-gcp.oesinc.dev keycloak.demo-gcp.oesinc.dev genesis.demo-gcp.oesinc.dev
#   192.168.49.2 gis.demo-gcp.oesinc.dev events.demo-gcp.oesinc.dev inventory.demo-gcp.oesinc.dev
#   192.168.49.2 event-creator.demo-gcp.oesinc.dev data-viewer.demo-gcp.oesinc.dev
#   192.168.49.2 der-dispatch.demo-gcp.oesinc.dev ess-manager.demo-gcp.oesinc.dev api.demo-gcp.oesinc.dev
#
ingress:
  enabled: true
  className: "nginx"
  # Base domain for all services
  # Development: demo-gcp.oesinc.dev (with /etc/hosts or DNS)
  # Production: your-domain.com (with real DNS)
  domain: "demo-gcp.oesinc.dev"
  # Service type for ingress controller
  # LoadBalancer: Production/cloud (automatic external IP, standard ports 80/443)
  # NodePort: Development/local (custom ports)
  serviceType: LoadBalancer
  # Port configuration
  # Only used when serviceType is NodePort
  ports:
    http: 80
    https: 443
  # Annotations for Ingress controller specific features
  annotations:
    # Increase timeouts for long-running requests
    nginx.ingress.kubernetes.io/proxy-connect-timeout: "300"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "300"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "300"
    # Keycloak can return large headers; increase buffer sizes
    nginx.ingress.kubernetes.io/proxy-buffer-size: "16k"
    nginx.ingress.kubernetes.io/proxy-buffers-number: "4"
    # Enable CORS for cross-subdomain requests
    # Note: CORS allow-origin is computed dynamically based on ingress.baseUrl
    nginx.ingress.kubernetes.io/enable-cors: "true"
    nginx.ingress.kubernetes.io/cors-allow-methods: "GET, POST, PUT, DELETE, OPTIONS"
    nginx.ingress.kubernetes.io/cors-allow-headers: "DNT,Keep-Alive,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range,Authorization"
    nginx.ingress.kubernetes.io/cors-allow-credentials: "true"
  # TLS configuration
  tls:
    enabled: true
    secretName: opendso-tls-secret # Default: <release-name>-tls-secret (overridden by deploy scripts)
    # cert-manager integration (optional, for automatic Let's Encrypt certificates)
    certManager:
      enabled: false # Set to true for automatic certificates in production
      issuerName: letsencrypt-prod # ClusterIssuer or Issuer name
      issuerKind: ClusterIssuer
# =============================================================================
# Subchart Configuration
# =============================================================================
# Values passed to specific subcharts
# These override subchart default values

# Keycloak Authentication Server
keycloak:
  config:
    # Hostname configuration - uses ingress domain
    hostname: ""  # Empty: Keycloak derives hostname dynamically via proxy=edge + hostname-strict=false
    # Port configuration - computed from ingress settings
    hostnamePort: "443"
    hostnameStrict: "false"
    hostnameStrictHttps: "false"
# NATS Messaging Server
nats:
  # WebSocket NodePort for browser access (development/local environments)
  websocketNodePort:
    enabled: false # Set to true in values-dev.yaml for local development
    port: 30009
# =============================================================================
# Grafana - Monitoring & Dashboards
# =============================================================================
grafana:
  # Use namespace-scoped Role/RoleBinding instead of ClusterRole/ClusterRoleBinding.
  # The deployer SA has only namespace-scoped permissions; a namespaced role also
  # provides the sidecar (grafana-sc-datasources) with the list-secrets permission it needs.
  rbac:
    create: true
    namespaced: true
  # Admin credentials — Grafana uses adminUser/adminPassword directly by default.
  # In production (deploy.sh), this is overridden with:
  #   --set grafana.admin.existingSecret=<release-name>-grafana-credentials
  adminUser: admin
  adminPassword: admin
  # Persistence
  persistence:
    enabled: true
    type: pvc
    storageClassName: standard
    size: 10Gi
  # Disable init container that changes ownership (causes permission issues on upgrade)
  initChownData:
    enabled: false
  # Service configuration
  service:
    type: ClusterIP
    port: 80
  # Ingress will be handled by parent chart
  ingress:
    enabled: false
  # Environment variables for datasource credentials
  # Secret name follows pattern: {{ .Release.Name }}-grafana-credentials
  # IMPORTANT: If using a custom release name, override with:
  #   --set grafana.envValueFrom.CITUS_PASSWORD.secretKeyRef.name=<your-release-name>-grafana-credentials
  #   --set grafana.envValueFrom.OPENDSO_APPS_DB_PASSWORD.secretKeyRef.name=<your-release-name>-grafana-credentials
  envValueFrom:
    CITUS_PASSWORD:
      secretKeyRef:
        name: opendso-grafana-credentials
        key: citus-password
        optional: true
    OPENDSO_APPS_DB_PASSWORD:
      secretKeyRef:
        name: opendso-grafana-credentials
        key: opendso-apps-db-password
        optional: true
  # Data sources are provisioned via grafana-datasources.yaml template (uses .Release.Name)
  sidecar:
    datasources:
      enabled: true
      label: grafana_datasource
      labelValue: "1"
  # Plugins to install
  plugins:
    - grafana-mongodb-datasource
  # Resource limits
  resources:
    limits:
      cpu: 500m
      memory: 512Mi
    requests:
      cpu: 250m
      memory: 256Mi
  # Environment variables
  env:
    GF_SECURITY_ALLOW_EMBEDDING: "true"
    GF_AUTH_ANONYMOUS_ENABLED: "false"
    GF_SERVER_ROOT_URL: "%(protocol)s://%(domain)s/"
    GF_SERVER_SERVE_FROM_SUB_PATH: "false"
  # Default dashboards
  dashboardProviders:
    dashboardproviders.yaml:
      apiVersion: 1
      providers:
        - name: 'default'
          orgId: 1
          folder: 'OpenDSO'
          type: file
          disableDeletion: false
          editable: true
          options:
            path: /var/lib/grafana/dashboards/default
  # Dashboard ConfigMaps (add your dashboards here)
  dashboards:
    default: {}
    # Example: Add dashboard ConfigMaps
    # system-metrics:
    #   file: dashboards/system-metrics.json
