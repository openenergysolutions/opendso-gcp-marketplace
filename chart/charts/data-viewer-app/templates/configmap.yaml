apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "data-viewer-app.fullname" . }}-nginx
  labels:
    {{- include "data-viewer-app.labels" . | nindent 4 }}
data:
  default.conf: |
{{ include "opendso.frontend.nginx" . | indent 4 }}
