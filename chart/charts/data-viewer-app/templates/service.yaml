apiVersion: v1
kind: Service
metadata:
  name: {{ include "data-viewer-app.fullname" . }}
  labels:
    {{- include "data-viewer-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: web
  selector:
    {{- include "data-viewer-app.selectorLabels" . | nindent 4 }}
