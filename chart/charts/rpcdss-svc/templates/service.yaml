apiVersion: v1
kind: Service
metadata:
  name: {{ include "rpcdss-svc.fullname" . }}
  labels:
    {{- include "rpcdss-svc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    {{- include "rpcdss-svc.selectorLabels" . | nindent 4 }}
