apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ahs-app.fullname" . }}-nginx
  labels:
    {{- include "ahs-app.labels" . | nindent 4 }}
data:
  default.conf: |
{{ include "opendso.frontend.nginx" . | indent 4 }}
