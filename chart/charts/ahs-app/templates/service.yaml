apiVersion: v1
kind: Service
metadata:
  name: {{ include "ahs-app.fullname" . }}
  labels:
    {{- include "ahs-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: web
  selector:
    {{- include "ahs-app.selectorLabels" . | nindent 4 }}
