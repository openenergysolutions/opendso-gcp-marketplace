apiVersion: v1
kind: Service
metadata:
  name: {{ include "event-viewer-app.fullname" . }}
  labels:
    {{- include "event-viewer-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: web
  selector:
    {{- include "event-viewer-app.selectorLabels" . | nindent 4 }}
