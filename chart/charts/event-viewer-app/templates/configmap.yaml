apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "event-viewer-app.fullname" . }}-nginx
  labels:
    {{- include "event-viewer-app.labels" . | nindent 4 }}
data:
  default.conf: |
{{ include "opendso.frontend.nginx" . | indent 4 }}
