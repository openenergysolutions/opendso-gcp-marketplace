apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "der-dispatch-app.fullname" . }}-nginx
  labels:
    {{- include "der-dispatch-app.labels" . | nindent 4 }}
data:
  default.conf: |
{{ include "opendso.frontend.nginx" . | indent 4 }}
