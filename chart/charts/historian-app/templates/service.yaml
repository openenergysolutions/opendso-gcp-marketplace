apiVersion: v1
kind: Service
metadata:
  name: {{ include "historian-app.fullname" . }}
  labels:
    {{- include "historian-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: web
  selector:
    {{- include "historian-app.selectorLabels" . | nindent 4 }}
