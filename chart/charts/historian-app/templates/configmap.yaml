apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "historian-app.fullname" . }}-nginx
  labels:
    {{- include "historian-app.labels" . | nindent 4 }}
data:
  default.conf: |
{{ include "opendso.frontend.nginx" . | indent 4 }}
