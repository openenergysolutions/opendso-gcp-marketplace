apiVersion: v1
kind: Service
metadata:
  name: {{ include "one-line-app.fullname" . }}
  labels:
    {{- include "one-line-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: web
  selector:
    {{- include "one-line-app.selectorLabels" . | nindent 4 }}
