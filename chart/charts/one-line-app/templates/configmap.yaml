apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "one-line-app.fullname" . }}-nginx
  labels:
    {{- include "one-line-app.labels" . | nindent 4 }}
data:
  default.conf: |
{{ include "opendso.frontend.nginx" . | indent 4 }}
