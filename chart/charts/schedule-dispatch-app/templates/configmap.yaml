apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "schedule-dispatch-app.fullname" . }}-nginx
  labels:
    {{- include "schedule-dispatch-app.labels" . | nindent 4 }}
data:
  default.conf: |
{{ include "opendso.frontend.nginx" . | indent 4 }}
