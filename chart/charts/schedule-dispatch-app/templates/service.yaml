apiVersion: v1
kind: Service
metadata:
  name: {{ include "schedule-dispatch-app.fullname" . }}
  labels:
    {{- include "schedule-dispatch-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: web
  selector:
    {{- include "schedule-dispatch-app.selectorLabels" . | nindent 4 }}
