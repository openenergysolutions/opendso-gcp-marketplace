apiVersion: v1
kind: Service
metadata:
  name: {{ include "cvr-openfmb-services-svc.fullname" . }}
  labels:
    {{- include "cvr-openfmb-services-svc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    {{- include "cvr-openfmb-services-svc.selectorLabels" . | nindent 4 }}
