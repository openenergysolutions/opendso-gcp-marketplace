apiVersion: v1
kind: Service
metadata:
  name: {{ include "cvr-genetic-algorithm-svc.fullname" . }}
  labels:
    {{- include "cvr-genetic-algorithm-svc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    {{- include "cvr-genetic-algorithm-svc.selectorLabels" . | nindent 4 }}
