apiVersion: v1
kind: Service
metadata:
  name: {{ include "opendso-docs-app.fullname" . }}
  labels:
    {{- include "opendso-docs-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: web
  selector:
    {{- include "opendso-docs-app.selectorLabels" . | nindent 4 }}
