apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "gis-app.fullname" . }}-nginx
  labels:
    {{- include "gis-app.labels" . | nindent 4 }}
data:
  default.conf: |
{{ include "opendso.frontend.nginx" . | indent 4 }}
