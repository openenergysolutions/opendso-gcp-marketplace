apiVersion: v1
kind: Service
metadata:
  name: {{ include "gis-app.fullname" . }}
  labels:
    {{- include "gis-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: web
  selector:
    {{- include "gis-app.selectorLabels" . | nindent 4 }}
