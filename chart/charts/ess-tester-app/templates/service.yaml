apiVersion: v1
kind: Service
metadata:
  name: {{ include "ess-tester-app.fullname" . }}
  labels:
    {{- include "ess-tester-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: web
  selector:
    {{- include "ess-tester-app.selectorLabels" . | nindent 4 }}
