apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ess-manager-app.fullname" . }}-nginx
  labels:
    {{- include "ess-manager-app.labels" . | nindent 4 }}
data:
  default.conf: |
{{ include "opendso.frontend.nginx" . | indent 4 }}
