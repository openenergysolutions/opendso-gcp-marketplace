apiVersion: v1
kind: Service
metadata:
  name: {{ include "ess-manager-svc.fullname" . }}
  labels:
    {{- include "ess-manager-svc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "ess-manager-svc.selectorLabels" . | nindent 4 }}
