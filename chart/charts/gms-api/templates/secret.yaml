{{- if .Values.config.mongodb.createSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "gms-api.fullname" . }}-mongodb
  labels:
    {{- include "gms-api.labels" . | nindent 4 }}
type: Opaque
stringData:
  mongodb-username: {{ required "gms-api MongoDB username is required when config.mongodb.createSecret=true" .Values.config.mongodb.username | quote }}
  mongodb-password: {{ required "gms-api MongoDB password is required when config.mongodb.createSecret=true" .Values.config.mongodb.password | quote }}
{{- end }}
