apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "inspector-app.fullname" . }}-nginx
  labels:
    {{- include "inspector-app.labels" . | nindent 4 }}
data:
  default.conf: |
{{ include "opendso.frontend.nginx" . | indent 4 }}
