apiVersion: v1
kind: Service
metadata:
  name: {{ include "inspector-app.fullname" . }}
  labels:
    {{- include "inspector-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: web
  selector:
    {{- include "inspector-app.selectorLabels" . | nindent 4 }}
